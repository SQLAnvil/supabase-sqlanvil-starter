config {
  type: "assertion",
  description: "Daily revenue must never be negative. Passes iff this query returns zero rows."
}

SELECT order_date, revenue_cents
FROM ${ref("daily_revenue")}
WHERE revenue_cents < 0
